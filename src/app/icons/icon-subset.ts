import {
  cilUser,
  cilHandshake,
  cilMobile,
  cilMonitor,
  cilCode,
  cilMenu
} from '@coreui/icons';

export const iconSubset = {
  cilUser,
  cilHandshake,
  cilMobile,
  cilMonitor,
  cilCode,
  cilMenu
};

export enum IconSubset {
  cilUser = 'cilUser',
  cilHandshake='cilHandshake',
  cilMobile='cilMobile',
  cilMonitor='cilMonitor',
  cilMenu='cilMenu'
  
}

